spring:
  datasource:
    url: jdbc:h2:mem:~/carx

  data:
    cassandra:
      contact-points: localhost
      port: 9042
      keyspace-name: carx
      schema-action: CREATE_IF_NOT_EXISTS
  jpa:
    database: h2
    hibernate:
      ddl-auto: none
